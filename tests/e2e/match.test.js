const req = require('../helpers/request');
const { assert } = require('chai');

describe('build user data', () => {

    // before(() => db.drop('users'));


    it('verify album data has length', () => {

    })
    it('verify user data has length', () => {

    })
    it('retrieve user and verify they have albums', () => {

    })
    it('retrieve user and create dictionary objects', () => {

    })

})