$('#search')
    .on('submit', (event) => {
        event.preventDefault();
        let searchObject = $('#search')
            .serializeArray();
        return searchObject = $('#search')
            .serializeArray();
    });